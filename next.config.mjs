/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Dias-Estilo",
  images: {
    unoptimized: true
  },
  trailingSlash: true
};

export default nextConfig;
